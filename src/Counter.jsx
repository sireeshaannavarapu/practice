import react from 'react'
import {connect} from 'react-redux'
import {decrementCount,incrementCount,resetCount} from './store/action'
function Counter(props){
    console.log("Counter props"::props)
    return(
        <div className
    )

    
}
